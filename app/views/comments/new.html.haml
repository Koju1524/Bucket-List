.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

= form_with(model: @comment, url: article_comments_path(@article), method: 'post', local: true) do |f|
  %div
    = f.label :cotent, '・Comment', class: 'title'
  %div
    = f.text_area :content, class: 'text'
    = f.submit 'Submit', class: 'btn-primary'