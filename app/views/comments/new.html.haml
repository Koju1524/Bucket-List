.container
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

= form_with(model: @comment, url: article_achieved_article_comments_path(@article, @achieved_article), method: 'post',local: true) do |f|
  %div
    = f.label :content, '・Comment', class: 'title'
  %div
    = f.text_area :content, class: 'text'
    = f.submit 'Submit', class: 'btn-primary'